const initialState = {
   
}

export default function enquiries(state = initialState, actions){

    switch(actions.type){
        case "":
            return state;
        default:
            return state;
           }
}